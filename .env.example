# Optional — only needed for the semantic judge's hosted NVIDIA NIM backend
# (--semantic-provider nvidia_nim). Local Ollama needs no key.
# Never commit a real key; export it in your shell instead of writing it here.
# NVIDIA_API_KEY=nvapi-...
